import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        name="search"
        id="search"
        className="search"
        placeholder="cari disini ..."
      />
    </div>
  );
};

export default SearchBar;
